// this is a comment 
 
 /* this is a 
longer comment */

color WHITE, BLACK;

//float

void setup()
{
  //conformes to 4:3 aspect ratio
  size(900,720);
  
  // 24 frame per second
  frameRate(24);
  
  WHITE = color(1.);
  BLACK = color(0.);
  
  rectMode(CENTER);
}

void draw()
{
  
}
